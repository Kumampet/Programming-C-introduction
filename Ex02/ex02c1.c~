#include <stdio.h>

int main()
{
  int a,b,c;
  printf("Enter numbers -> ");
  scanf("%d%d%d",&a,&b,&c);

  printf("合計 = %d\n",a+b+c);
  printf("平均（商） = %d\n",(a+b+c)/3);
  printf("平均（余り） = %d\n",(a+b+c)%3);

  return 0;
}
