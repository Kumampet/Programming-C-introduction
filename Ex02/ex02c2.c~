#include <stdio.h>

int main()
{
  int a,b,c;
  printf("縦の長さを入力してください");
  scanf("%d",&a);
  printf("横の長さを入力してください");
  scanf("%d",&b);
  printf("高さを入力してください");
  scanf("%d",&c);

  printf("体積 = %d\n",a+b+c);
  printf("平均（商） = %d\n",(a+b+c)/3);

  return 0;
}
