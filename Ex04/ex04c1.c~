/*以下のプログラム中の if 文の条件式を a=b に書き換え、a とb に対して下記の４種類の値のペアを与えたとき、どのような結果が得られるかを提出ファイルに記述しなさい。
さらに、何故そのような結果が得られるのか、説明を記述しなさい。
ヒント： if 文の条件式を書き換えずにそのまま実行した場合の結果と異なることを確認し、書き換え前後で条件式の意味や値がどのように変わったか考えてください.*/

/* 条件式の間違いによる影響を調べるためのプログラム */
/* 作成者：                                */
/* 作成日：                                */
#include <stdio.h>
int main()
{
  int a, b;

  printf("a と b を入力して下さい> ");
  scanf("%d%d", &a, &b);
  printf("a=%d, b=%d\n", a, b);

  if ( a == b ) printf("a と b は等しい\n");
  else printf("a と b は等しくない\n");
  printf("a=%d, b=%d\n", a, b);
  return 0;
}
